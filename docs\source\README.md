# Source documents

This directory preserves the supplied semester-project report and presentation as historical source material.

These files may contain formatting metadata, tracked edits, duplicated figures, and claims that conflict with one another. The curated Markdown documentation at the repository root resolves or flags those conflicts; it does not silently treat every source statement as verified.

| File | Purpose |
|---|---|
| `Embedded-Systems-Report.docx` | Original detailed semester report |
| `Ultra-Precision-Current-and-Voltage-Measurement-System.pptx` | Original project presentation |
