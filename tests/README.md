# Tests and calibration

No raw calibration dataset, serial capture, or automated test script was supplied. Add them here in reviewable formats.

Recommended structure:

```text
tests/
├── calibration/   # Raw and processed CSV data
├── results/       # Test reports, plots, and serial captures
└── tools/         # Reproducible parsers and analysis scripts
```

Do not commit only screenshots of measurements when the underlying numeric data is available.
